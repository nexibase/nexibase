import type { Plugin } from '@/types/plugin';

const plugin: Plugin = {
  name: 'Countdown Timer',
  description: 'Display countdown timers for important events',
  version: '1.0.0',
  author: 'Nexibase',
  slug: 'countdown-timer',
  defaultEnabled: true,
};

export default plugin;
